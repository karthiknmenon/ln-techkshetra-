'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

exports.createManagementClient = createManagementClient;

var _contentfulManagement = require('contentful-management');

var _package = require('../../package.json');

var _context = require('../context');

var _proxy = require('./proxy');

async function createManagementClient(params) {
  params.application = `contentful.cli/${_package.version}`;

  const context = await (0, _context.getContext)();
  const { rawProxy, proxy, host } = context;

  const proxyConfig = {};
  if (!rawProxy) {
    const { httpsAgent } = (0, _proxy.agentFromProxy)(proxy);
    proxyConfig.httpsAgent = httpsAgent;
  } else {
    proxyConfig.proxy = proxy;
  }

  return (0, _contentfulManagement.createClient)(_extends({}, params, proxyConfig, { host }));
}