'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _scopes = require('../../events/scopes');

var _inquirer = require('inquirer');

var _inquirer2 = _interopRequireDefault(_inquirer);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const createCreateSpaceHandlerIntents = config => {
  const createSpaceHandlerIntents = {
    scopes: [_scopes.CREATE_SPACE_HANDLER],
    intents: {
      'SELECT_ORG': async ({ organizations }) => {
        const answersOrganizationSelection = await _inquirer2.default.prompt([{
          type: 'list',
          name: 'organizationId',
          message: 'Please select an organization:',
          choices: organizations
        }]);

        return answersOrganizationSelection.organizationId;
      }
    }
  };

  return createSpaceHandlerIntents;
};

exports.default = createCreateSpaceHandlerIntents;